# セキュリティレビュー — 請求書ジェネレーター

最終実施日: 2026-07-31(第3回・収入印紙 判定ツールの追加分)
第2回: 2026-07-30(解説記事とURLパラメータの追加分)
初回実施日: 2026-07-29(公開前レビュー・第1回)
対象コミット: 初版(`public/` 一式・`vercel.json`)+ 解説記事3ページ・`?type=` パラメータ
+ 収入印紙 判定ツール(`stamp.js` / `inshi.js` / `inshi.html`)

## 前提となる設計

本ツールはサーバー側の処理を一切持たない静的サイトである。入力値は**利用者のブラウザ内でのみ**扱われ、
ネットワークへ送出する経路そのものが存在しない。これが最大の防御になっている。

| 項目 | 状態 |
|------|------|
| バックエンドAPI | なし |
| 外部への通信 | なし(`fetch` / `XMLHttpRequest` を一切使用しない) |
| 外部スクリプト・CDN | なし(依存パッケージ0) |
| Cookie | 使用しない |
| 保存先 | 端末の localStorage のみ |
| 認証・決済 | 未実装(有料機能は未着手) |

## 確認項目

### 1. XSS(クロスサイトスクリプティング)

`innerHTML` を使う箇所は次の4つ。それぞれ入力の出どころを確認した。

| 箇所 | 埋め込む値 | 対策 |
|------|-----------|------|
| `createItemRow` | 固定のテンプレート文字列のみ。利用者の入力は `.value` 経由で代入 | 値をHTMLに連結しない |
| `renderPreview` の明細 | 品目名・単位 | `escapeHtml()` で `& < > " '` をエスケープ |
| `renderPreview` の合計欄 | 数値のみ(`yen()` の出力) | 文字列を受け付けない |
| `renderPreview` の税率内訳 | 数値のみ | 同上 |

宛名・住所・備考など残りの自由入力欄は、すべて `textContent`(`setText()`)で描画しており、
HTMLとして解釈されない。**インラインイベントハンドラ・`eval`・`new Function` は不使用。**

### 2. コンテンツセキュリティポリシー

`vercel.json` で以下を全パスに付与する。

```
default-src 'none'; script-src 'self'; style-src 'self'; img-src 'self' data:;
font-src 'self'; connect-src 'none'; form-action 'none'; base-uri 'none'; frame-ancestors 'none'
```

- `connect-src 'none'` — 万一スクリプトが混入しても、外部への送信ができない(入力内容の流出防止)
- `script-src 'self'` — インラインスクリプトを禁止。実際にインラインスクリプトは1つも使っていない
- `frame-ancestors 'none'` / `X-Frame-Options: DENY` — クリックジャッキング対策

あわせて `X-Content-Type-Options: nosniff`、`Referrer-Policy: strict-origin-when-cross-origin`、
`Permissions-Policy`(位置情報・マイク・カメラを無効化)を付与している。

### 3. 外部から取り込むデータ

「JSONを読み込む」機能は利用者自身が選んだファイルを読む。悪意あるJSONを読ませられた場合を想定し、

- 値はすべて `.value` またはエスケープ済みの描画経路に流れる(スクリプトにならない)
- `docType` は `hasOwnProperty` で既知のキーのみ採用し、未知の値は「請求書」に落とす
- パース失敗時は例外を捕捉してメッセージを出すだけで、処理を継続しない

### 4. 保存データの取り扱い

- 下書きは localStorage の `invoice-tool:draft:v1` にのみ保存される
- 「入力をすべて消す」で削除できる。この動作は `/legal.html` に明記した
- 共用端末での注意喚起も同ページに記載
- localStorage が使えない環境(プライベートブラウズ等)でも例外を捕捉し、機能は継続する

### 5. 依存関係

外部パッケージを使用していないため、サプライチェーン経由の脆弱混入の経路がない。

## 第2回レビュー(2026-07-30)— 差分のみ

### 追加したもの

| 追加物 | 種類 | リスク評価 |
|--------|------|-----------|
| `public/guide/index.html`、`guide/seikyusho-kakikata.html`、`guide/gensen-choshu-keisan.html`、`mitsumorisho.html` | 静的HTML | スクリプト・フォーム・外部リソースを一切含まない。インラインスクリプト0・`onclick` 等の属性0 |
| `public/robots.txt` | テキスト | 公開情報のみ。sitemapの絶対URLは本番ドメイン確定後に追記する(存在しないURLは書かない) |
| `?type=` による書類種別の指定 | JavaScript | 下記のとおり検証済み |

### `?type=` パラメータの検証

`requestedDocType()` は `URLSearchParams` で取り出した値を、`DOC_TYPE_BY_QUERY` の
**キーとの完全一致でしか採用しない**(`hasOwnProperty` で判定)。一致しなければ `null` を返し、
既定の「請求書」のままになる。

- URLの文字列がDOMへ到達する経路がない(採用されるのはコード内の定数文字列のみ)
- `<select>` の `value` に代入するため、既存の選択肢にない値は反映されない
- プロトタイプ汚染("`__proto__`" 等)は `hasOwnProperty` 判定で弾かれる

**結論: 反射型XSSおよびオープンリダイレクトの経路は生じていない。**

### CSPとの整合

記事ページはインラインスクリプト・インラインスタイルを使わず、`/style.css` のみを読み込む。
第1回で設定した `default-src 'none'` のCSPを緩める必要は生じていない。**`vercel.json` は変更なし。**

### 記事本文の記載内容

外部サイトへのリンクは張っていない(制度の出典はタックスアンサーの番号と名称のみを本文に記載)。
**存在しないURLを記事に書かない方針を守っている。**

## 第3回レビュー(2026-07-31)— 収入印紙 判定ツールの追加分

### 追加したもの

| 追加物 | 種類 | リスク評価 |
|--------|------|-----------|
| `public/stamp.js` | JavaScript(純粋関数) | DOMに触れない。入力は数値と列挙値のみ。外部通信なし |
| `public/inshi.js` | JavaScript(画面制御) | `innerHTML` と URLパラメータを使うため下記のとおり個別に検証 |
| `public/inshi.html` | 静的HTML | インラインスクリプト0・`onclick` 等の属性0・外部リソース0 |
| `app.js` の `updateStampHint()` | JavaScript | 生成するURLは `URLSearchParams` 経由。値は自前の計算結果(数値)のみ |

### `inshi.js` の `innerHTML` の検証

結果パネルの骨組みだけを `innerHTML` で作り、**文言は例外なく `textContent` で後から入れている**。

| 埋め込む値 | 経路 | 判定 |
|-----------|------|------|
| 判定結果の見出し(「200円の収入印紙が必要です」等) | `innerHTML` | 値は `stamp.js` の**固定の税額表**から引いた数値のみ。利用者の入力文字列は含まれない |
| `result.reason` / `result.basisLabel` | `textContent` | HTMLとして解釈されない |
| `result.notes[]` | `createElement` + `textContent` | 同上 |
| 記載金額・消費税額 | `toLocaleString()` の出力(数値) | 文字列を受け付けない |

税額表の描画(`renderTable`)も `createElement` + `textContent` で行い、**数値をHTMLに直書きしていない**
(判定ロジックと表示が同じ表を参照するため、片方だけ古くなる事故も防いでいる)。

### `?amount=` / `?rate=` パラメータの検証

請求書ジェネレーターの領収書モードから金額を引き継ぐために追加した。

- `amount` — `Number()` で数値化し、**有限かつ0以上のときだけ**採用する。`Math.floor()` を通して
  `<input type="number">` の `value` に代入する。DOMへは数値としてしか到達しない
- `rate` — `<select>` の**実在する選択肢の値との完全一致でしか採用しない**(ホワイトリスト方式)。
  一致しなければ既定の10%のまま
- どちらも `innerHTML` に流れる経路がない

**結論: 反射型XSSの経路は生じていない。**

### CSPとの整合

`inshi.html` はインラインスクリプト・インラインスタイルを使わず、`/style.css` と自前の
`/stamp.js` `/inshi.js` のみを読み込む。**`vercel.json` は変更なし**(`script-src 'self'` のまま)。

### 判定内容そのものの安全性(税務上のリスク)

コードの脆弱性ではないが、**誤った判定は利用者の金銭的損害に直結する**ため、あわせて確認した。

- 税額表は国税庁 No.7141 の原文と**全15区分を照合済み**(2026-07-31)
- **5万円ちょうどの境界**は、テストで誤りを検出して修正した(当初「5万円ちょうど=非課税」と誤判定していた)
- **電子交付・クレジットカード決済の取扱いは一次資料での裏取りができていない。**
  そのため判定結果に必ず「一次資料で確認できていない/税務署に確認を」の注意書きを添える実装にし、
  テストでもその文言の存在を検証している(`stamp.test.js`)
- 全ページ共通のフッタに「税務判断を行うものではない」旨を記載済み

## 残課題(有料機能の実装前に必ず再レビューする)

1. **Stripe組込み時**: 決済ページでは `connect-src` に Stripe のエンドポイントを追加する必要があり、
   `script-src` も緩める判断が要る。CSPを決済ページ限定で分離すること。
2. **サーバー保存機能(取引先マスタ・発行履歴)を作る場合**: 取引先名・金額という機微情報を
   預かることになる。現在の「サーバーに何も送らない」という約束と `/legal.html` の記載を
   必ず同時に見直す。端末内保存で完結できる設計を優先して検討する。
3. **広告を導入する場合**: 第三者スクリプトの読み込みはCSPの緩和を伴う。導入可否は
   「入力内容が第三者に渡らないこと」を条件に判断する。

## 結論

現時点の実装(静的サイト・外部通信なし・決済なし・解説記事3ページを含む)について、
公開を妨げる問題は確認されなかった。ただし**本番公開はオーナー承認後**に行う。

---

## 第4回(2026-08-01・公開後の追加ページ `/ryoshusho`)

**対象**: 領収書の入口ページ `public/ryoshusho.html`、および全ページのナビ・フッタ・
`sitemap.xml` / `robots.txt` / `canonical` の追加分。

| 観点 | 結果 |
|---|---|
| インラインの `<script>` / `style=` 属性 | **0件**。CSP `script-src 'self'; style-src 'self'` に適合する |
| 外部ホストへの参照(CDN・フォント・画像・計測) | **0件**。自ドメインの絶対URLが `canonical` の1件のみ |
| フォーム・入力欄・`connect-src` を要する処理 | **なし**(静的な解説ページ。JSを一切読み込まない) |
| 利用者の入力を扱う箇所 | **なし**。既存の判定・計算ロジックには変更を加えていない |
| 既存テスト | **33件(calc 16 / stamp 17)全通過**。ロジック未変更のため回帰なし |

**`canonical` と `sitemap.xml` の追加について**: どちらも本番ドメイン
`https://invoice-tool-kohl.vercel.app` が確定したことで初めて書けるようになったもの。
存在しないURLを書かないという方針に沿って、公開前は意図的に保留していた。
`sitemap.xml` に収録したのは検索対象の7URLで、`noindex` を指定している `/legal` は含めない。

### 判定内容そのものの安全性(記述の正確性)

- 適格簡易請求書を**交付できる事業の範囲**と**記載事項(宛名不要・税率か消費税額のいずれか一方)**は、
  国税庁「消費税の仕入税額控除制度における適格請求書等保存方式に関するQ&A」**問24・問58** で確認した
- 収入印紙の論点は本ページで断定せず、**要点のみ示して `/inshi` に委譲**している。
  「電子交付・クレジットカードは一次資料での裏取りができていない」という第3回の判断は
  `/inshi` 側の注意書きで維持されており、本ページはそれと矛盾する断定をしていない
- 「宛名の書き換えは行わず作り直す」「再発行には再発行と明記する」等の実務上の助言は、
  **税務判断ではなく一般的な運用の説明**として書き、フッタの免責を全ページ共通で表示している

### 結論

公開を妨げる問題は確認されなかった。本ページは**費用0円・課金機能なし・外部通信なし・レビュー済み**で、
2026-07-31 のオーナー包括承認(3条件を満たす公開は都度承認不要)の範囲内であるため、本番公開を実行した。

## 第5回(2026-08-17・OGP/`twitter:card` の補修と Xシェアリンクの追加分)

対象は本日の差分のみ(全8ページの `<head>` に4行のメタタグ / `index.html` にシェアリンク1本 /
`app.js` の `btnPrint` ハンドラ / `style.css` に見た目の定義)。**計算・判定ロジックは1行も変更していない。**

| 確認項目 | 結果 |
|---|---|
| メタタグの中身 | **既存の `<title>` と `meta description` の複製のみ**。新しい文言・外部URL・画像参照は0件 |
| `og:image` | **入れていない**。外部ホストの参照も新規アセットも発生しない |
| シェアリンクに載せる情報 | **固定の文言とサイトURLのみ**。**取引先名・金額・登録番号などの入力内容は一切含めない**(URLクエリに個人情報を置かない) |
| 送信処理 | **0件**。`x.com/intent/post` を**開くだけ**のリンクで、APIキー・アカウント・自動投稿はいずれも無い。押すかどうかは利用者が決める |
| リンクの属性 | `target="_blank"` に **`rel="noopener noreferrer"`** を付与(reverse tabnabbing と Referrer 漏れの対策) |
| CSP との整合 | `default-src 'none'` は**リンクによる遷移を制限しない**(`connect-src` / `form-action` を使う処理は追加していない)。インライン `<script>` / `style=` の追加も0件 |
| PDFへの混入 | シェア行は **`no-print`** で、印刷時に `display: none` になる(既存の `@media print` 規則を再利用) |
| 既定の表示状態 | `hidden` 属性で非表示。**PDFを出した直後だけ**表示する(`[hidden] { display: none !important }` が既に定義済み) |
| 既存テスト | **33件(calc 16 / stamp 17)全通過**。ロジック未変更のため回帰なし |

### 結論

公開を妨げる問題は確認されなかった。**費用0円・課金機能なし・外部への送信なし・レビュー済み**で、
2026-07-31 のオーナー包括承認の範囲内。シェアリンクについては
統括本部が 2026-08-05 に「Xアカウントの保有が不要で包括承認の範囲」と判断済みである。
